/** @type {import('@marp-team/marp-cli').Config} */
export default {
  allowLocalFiles: true,
  html: false,
  lang: "ja",
  themeSet: "./theme",
  options: {
    markdown: {
      breaks: false,
    },
  },
};
