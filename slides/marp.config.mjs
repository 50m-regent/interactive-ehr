/** @type {import('@marp-team/marp-cli').Config} */
export default {
  allowLocalFiles: true,
  html: true,
  lang: "ja",
  themeSet: "./theme",
  options: {
    markdown: {
      breaks: false,
    },
  },
}
