[project]
name = "interactive-ehr"
version = "0.1.0"
description = "タスクのグラフ構造化を用いたインタラクティブな電子カルテシステム"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "streamlit>=1.56.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
